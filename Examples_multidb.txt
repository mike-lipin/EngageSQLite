ѕодключить ещЄ одну базу просто, надо выполнить sql запрос:

attach database 'fullpathtodb\my.db' as dbalias;

ѕосле этого у вас будет подключено 2 базы данных
1. “а база к которой вы открывали подключение изначально, еЄ псевдоним main
2. “а база, которую вы подключили сейчас запросом, еЄ псевдоним dbalias (можте при подключении сами задавать алиас)

Ќаписание запросов к 2 базам практически ничем не отличаетс€ от запросов к одной базе.
  примеру в main базе есть табличка Event, а в dbalias базе есть табличка EventDescription с пол€ми: EventId и Description
тогда пишем запрос:

select e.Id, e.Name, ed.Description
from Event e
  join EventDescription ed on ed.EventId = e.Id

( райне желательно использвать алиасы дл€ таблиц в запросах, чтобы не было путаницы с выбором пол€, если в разных таблицах пол€ называютс€ одинаково.)
ƒанный запрос без проблем работает, если у нас нет пересечений по именам таблиц.

¬ случае, если таблица с одним и тем же названием существует и в первой базе и во второй, то необходимо использовать алиас базы, 
чтобы SQLite пон€л, какую же таблицы вы хотите использовать.
“.е. если в обоих базах есть таблица EventDescription, то приведЄнный выше запрос, выберет данные из базы main, 
а чтобы он брал данные из базы dbalias, но саму таблицу Event из базы main, то указываем алиасы:

select e.Id, e.Name, ed.Description
from main.Event e
  join dbalias.EventDescription ed on ed.EventId = e.Id



ѕример, создаЄм подключение к базе Sample.db, далее подключаем базу Test:
attach database 'dbfolder\Test.db' as dbalias;

ѕровер€ем запрос:
select e.Id, e.Name, ed.Description
from Event e
  join EventDescription ed on ed.EventId = e.Id

должен вернуть 1 запись, при этом таблица Event у нас в базе Sample.db, а таблица EventDescription в базе Test.db

далее делаем запрос:
select * from EventItem

должно вернуть 100 записей с кучей полей из таблицы EventItem, котора€ лежит в базе Sample.db

далее делаем запрос:
select * from dbalias.EventItem

должно вернуть всего 1 запись и с одним полем, т.к. это содержимое из таблицы EventItem, котора€ лежит в базе Test.db

Test.db - можно найти в этой же папке Dropbox